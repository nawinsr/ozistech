import { IfellipsisDirective } from './ifellipsis.directive';

describe('IfellipsisDirective', () => {
  it('should create an instance', () => {
    const directive = new IfellipsisDirective();
    expect(directive).toBeTruthy();
  });
});
